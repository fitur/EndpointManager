```
WEYLAND-YUTANI CORP // NETWORK DIVISION
MU/TH/UR 6000 :: ENDPOINT CONTROL SUBSYSTEM
REF EM-2179 — CLEARANCE: GENERAL — STATUS: ONLINE
```

# ENDPOINT MANAGER

An automation lattice for the estate. Each module governs the machines from
genesis to retirement — provisioning, inventory, and the quiet enforcement of
policy. The crew need not know it runs. It simply runs.

> _"We process the fleet so the fleet need not process itself."_

## ACTIVE SYSTEMS

Current instruments. Each occupies its own cell and carries its own briefing.

| MODULE | MANDATE |
| --- | --- |
| `Intune/Export-IntuneConfigurationInventory/` | The ledger. Reads the tenant whole, then names what drifted between one reading and the next. |
| `Intune/New-IntuneWin32AppJson/` | The forge. A packaged application enters; a published, assigned artifact leaves. |
| `Intune/Rename-IntunePolicies/` | The nomenclator. Imposes one naming order upon the tenant. |

## COLD STORAGE

Decommissioned but preserved. Retained for reference; not maintained. Expect
site designators and dependencies long since retired.

| VAULT | CONTENTS |
| --- | --- |
| `Archive/ActiveDirectory/` | The old registry of identity — policy survey, attribute synchronisation, firmware directives. |
| `Archive/Azure/` | Retrieval from blob storage. |
| `Archive/Intune/Remediations/` | Autonomous correction. Detect the fault; excise the fault. Nine matched pairs and one orphan. |

## DIRECTIVE

No credentials reside herein — supply them through the environment. Re-map every
site designator before execution. Rehearse against a closed cell; the live
estate is unforgiving. Distributed under MIT. No warranty. No exceptions.

```
TRANSMIT: fitur@duck.com
BUILDING BETTER WORLDS
END OF LINE
```
